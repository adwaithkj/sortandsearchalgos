# Given a list find the number of elements in the lists which is
# a multiple or a factor of the number for each element with
# a time complexity less than O(n^2)


def divisible(arr):
    newdict = {}
    for i in arr:
        newdict[i] = arr
    for i in newdict:
        for j in newdict[i]:


if __name__ == '__main__':
    arr = [2, 3, 5, 4, 7, 5, 14, 6, 8, 16, 9, 10]
